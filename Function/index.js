// Viết hàm tính diện tích tam giác
const dienTichTamGiac = (a, h) => {
  return (a * h) / 2;
};
console.log(dienTichTamGiac(10, 5));

// Viết hàm tính diện tích khối hộp
const dienTichKhoiHop = (w, h, l) => {
  return w * h * l;
};
console.log(dienTichKhoiHop(10, 20, 30));
